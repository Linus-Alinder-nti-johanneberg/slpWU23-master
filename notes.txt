/* make a checkbox that hides a red box if the checkbox is checked with a 1 second transition */
var checkbox = document.createElement('input');
checkbox.type = 'checkbox';
document.body.appendChild(checkbox);
var redBox = document.createElement('div');
redBox.style.width = '100px';
redBox.style.height = '100px';
redBox.style.backgroundColor = 'red';
redBox.style.transition = 'opacity 1s';
document.body.appendChild(redBox);
checkbox.addEventListener('change', function() {
  if (checkbox.checked) {
    redBox.style.opacity = 0;
  } else {
    redBox.style.opacity = 1;
  }
});
/* make it so it is in reverse */
checkbox.addEventListener('change', function() {
  if (checkbox.checked) {
    redBox.style.opacity = 1;
  } else {
    redBox.style.opacity = 0;
  }
});



Here is a simplified running example of Seattle Ninja's solution for javascipt fade layer.

var slideSource = document.getElementById('slideSource');

document.getElementById('handle').onclick = function () {
  slideSource.classList.toggle('fade');
}
#slideSource {
  opacity: 1;
  transition: opacity 1s; 
}

#slideSource.fade {
  opacity: 0;
}
<button id="handle">Fade</button> 
<div id="slideSource">Whatever you want here - images or text</div>

// email contact box html?

